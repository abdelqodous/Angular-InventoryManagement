import { RelativeDatePipePipe } from './relative-date-pipe.pipe';

describe('RelativeDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new RelativeDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
